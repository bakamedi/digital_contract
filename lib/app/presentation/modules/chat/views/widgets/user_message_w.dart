import 'package:flutter/material.dart';

class UserMessageW extends StatelessWidget {
  const UserMessageW({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
